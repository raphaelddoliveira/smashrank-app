name: ats_ranking
description: Sistema de Ranking ATS 2026 - Gerenciamento de ranking de tenis
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # State Management
  flutter_riverpod: ^2.6.1

  # Supabase
  supabase_flutter: ^2.8.0

  # Navigation
  go_router: ^14.8.1

  # Serialization
  json_annotation: ^4.9.0

  # UI
  google_fonts: ^6.2.1
  fl_chart: ^0.70.2
  table_calendar: ^3.2.0
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0

  # Utilities
  flutter_dotenv: ^5.2.1
  url_launcher: ^6.3.1
  image_picker: ^1.1.2
  image_cropper: ^8.0.2
  share_plus: ^10.1.4
  intl: ^0.20.2

  # Auth
  google_sign_in: ^6.2.2
  sign_in_with_apple: ^6.1.3

  # Deep Linking
  app_links: ^6.3.3

  # Local Storage
  shared_preferences: ^2.3.5

  # Connectivity
  connectivity_plus: ^6.1.1

  # Firebase (Web Push Notifications)
  firebase_core: ^3.12.1
  firebase_messaging: ^15.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  # Testing
  mocktail: ^1.0.4

flutter:
  uses-material-design: true

  assets:
    - .env
